using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    [SerializeField] private int energiaActual=1000;
    [SerializeField] private int costoMagia=250;

    void Start()
    {
      energiaActual = energiaActual-costoMagia;

        print("Energia Actual =  " + energiaActual);

    }

    // Update is called once per frame
    void Update()
    {

    }
}
